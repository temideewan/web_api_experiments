// 1 create a div that serves as the mount point
// create the function that mounts the app exported by the zoid component
// It should receive an object as a parameter
  // the object should have the following properties
    // amount
